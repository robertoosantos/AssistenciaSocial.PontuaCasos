@model AssistenciaSocial.PontuaCasos.WebApp.Models.IndividuoEmViolacao

@{
    ViewData["Title"] = "Remover Pessoa";
}

<h1>Remover</h1>

<h3>Tem certeza que deseja remover esta pessoa?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Item.Titulo)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Item.Titulo)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Remover" class="btn btn-danger" /> |
        <a asp-action="Details" asp-controller="Casos" asp-route-id="@Model.CasoId">Voltar ao Caso</a>
    </form>
</div>
