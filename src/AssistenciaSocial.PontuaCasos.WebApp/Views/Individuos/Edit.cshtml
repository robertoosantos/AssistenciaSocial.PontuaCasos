@model AssistenciaSocial.PontuaCasos.WebApp.Models.IndividuoEmViolacao

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Indivíduo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Item.Titulo)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Item.Titulo)
            </dd>
        </dl>
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            @foreach (var item in Model.ViolenciasSofridas)
            {
                <partial name="~/Views/Shared/_ItemSelect.cshtml" model="@Model.OpcoesViolencias" />
            }
            @foreach (var item in Model.SituacoesDeSaude)
            {
                <partial name="~/Views/Shared/_ItemSelect.cshtml" model="@Model.OpcoesSaude" />
            }
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
